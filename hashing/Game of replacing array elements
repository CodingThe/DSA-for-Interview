#include<bits/stdc++.h>
using namespace std;
void vow(vector<int>&v){
    set<int>s(v.begin(),v.end());
    if(s.size()%2==0){
        cout<<"P won"<<endl;
    }
    else{
        cout<<"P hi won"<<endl;
    }
}
int main(){
    int n;
    cin>>n;
    vector<int>v;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        v.push_back(x);
    }
    vow(v);
}
